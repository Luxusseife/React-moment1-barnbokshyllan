interface BookInfo {
    author: string,
    illustrator: string,
    publisher: string,
    publicated: number,
    read: boolean
}

function Book({author, illustrator, publisher, publicated, read} : BookInfo) {
  return (
    <div>
      
    </div>
  )
}

export default Book
